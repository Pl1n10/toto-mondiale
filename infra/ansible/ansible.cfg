[defaults]
inventory = inventory.ini
roles_path = roles
host_key_checking = False
retry_files_enabled = False
stdout_callback = yaml
interpreter_python = auto_silent

[ssh_connection]
# Reach the VM over Tailscale SSH. Pipelining speeds up multi-task plays.
pipelining = True
